
func Hello(string name) {
  paral {
    seq {
      if(Rand() > 0.5) {
        Trace("Bye, " + name)
        RUNNING()
      } else {
        Trace("Hello, " + name)
        RUNNING()
      }
    }
    Wait(0.5)
  }
}
