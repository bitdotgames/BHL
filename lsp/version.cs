namespace bhl.lsp;

public static class Version
{
  // Stamped to the 'lsp-vX.Y.Z' tag by the release pipeline (see release_lsp.yml).
  // Stays "dev" for local/CI dev builds.
  public static string Name = "dev";
}
