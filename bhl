#!/bin/bash

set -e

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
cd $DIR

SRC=bhl.cs
EXE=bhl.exe

if [ $EXE -ot $SRC ]; then 
  mcs $SRC -debug -r:mono_opts.dll -out:$EXE
fi

mono --debug $EXE $*
